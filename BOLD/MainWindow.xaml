<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="BOLD.MainWindow"
        Title="BOLD" Height="350" Width="345"
        >

    <Grid>
        <Menu VerticalAlignment="Top" >
            <MenuItem Header="_File"   >
                <MenuItem Header="_New..." />
                <Separator />
                <MenuItem Header="_Add Image..." Click="Add_Click" />
                <MenuItem Header="_Replace Image..."  />
                <Separator />
                <MenuItem Header="_Save" />
                <MenuItem Header="_Save As..." />
                <Separator />
                <MenuItem Header="_Exit" Click="Exit_Click" />
            </MenuItem>
        </Menu>
        <Image Margin="20,57,50,50" Name="image" />
        <StackPanel Orientation="Horizontal" Margin="0,20,0,0" VerticalAlignment="Top">
            <TextBox x:Name="txtNum" x:FieldModifier="private" Margin="5,5,0,5" Width="30" Text="0" TextChanged="txtNum_TextChanged" />
            <Button x:Name="cmdUp" x:FieldModifier="private" Margin="5,5,0,5" Content="˄" Width="20" Click="cmdUp_Click" />
            <Button x:Name="cmdDown" x:FieldModifier="private" Margin="0,5,0,5"  Content="˅" Width="20" Click="cmdDown_Click" />
        </StackPanel>
    </Grid>

</Window>
